import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFFC19E67); // Gold/Sand color for a premium feel
  static const Color primaryDark = Color(0xFF1F222B); // Dark slate/Blue for backgrounds
  
  // Secondary / Accent colors
  static const Color accent = Color(0xFFE8E8E8);
  static const Color secondary = Color(0xFF707070);

  // Neutral colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);
  
  // Error / Success
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
}
